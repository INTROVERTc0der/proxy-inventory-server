{
  "name": "proxy-distributor-inventory-server",
  "version": "1.0.0",
  "description": "Proxy distributor inventory server",
  "main": "proxy.js",
  "type": "module",
  "scripts": {
    "start": "node proxy.js",
    "build": "echo \"No build step required\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["proxy", "inventory", "server"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^2.6.7"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
